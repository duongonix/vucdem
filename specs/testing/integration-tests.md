# Integration Tests

Trusted Firestore transactions, counter idempotency, ownership, moderator/admin role checks,
and direct-client denial should be tested with Firebase Emulator Suite before production.
The current local environment does not bundle the emulator runtime.
