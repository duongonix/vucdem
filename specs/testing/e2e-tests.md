# E2E Tests

Playwright tests start the production preview and verify core anonymous UI, mobile layouts,
dialogs, Search discovery entry points, and protected-route redirects. Authenticated write
workflows require deterministic emulator identities and are deferred to the emulator suite.
