# Phase 18 — Create Story UI

## Status

Completed

## Tasks đã hoàn thành

Added Story mode to `/write`, metadata form, bounded tags, signed Cloudinary cover upload, draft creation, unsaved-change guard, and redirect to manager.

## Files Changed

`src/lib/components/story/CreateStoryEditor.svelte`, `StoryCoverUploader.svelte`, `StoryTagEditor.svelte`, `src/routes/write/+page.svelte`

## Dependencies Added

None.

## Database Changes

Uses existing Story schema and atomic `storySlugs`; initializes `chapterSequence`.

## Security Changes

Authenticated server creation; Cloudinary secret remains server-only.

## Specs Added or Updated

Added story editor contract and documented chapter allocator.

## Important Decisions

Story and media IDs are reserved before upload; new stories always start as drafts.

## Known Limitations

Live upload requires configured Firebase and Cloudinary credentials.

## Verification

Formatter, Svelte type-check, ESLint, unit tests, production build, API authorization smoke checks, and responsive browser smoke checks completed successfully.
