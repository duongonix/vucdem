# Phase 24 — Communities

## Status

Completed

## Tasks đã hoàn thành

Added Community validation, service and APIs, unique slug reservation, membership, trusted counters, public responsive page, post feed filter, and Community selection in post editor.

## Files Changed

`src/lib/validation/community.ts`, `src/lib/server/communities.ts`, `src/lib/services/communities.ts`, `src/routes/api/communities/**`, `src/lib/components/community/CommunityPage.svelte`, `src/routes/c/[slug]/+page.svelte`

## Dependencies Added

None.

## Database Changes

Added `communitySlugs` and `communities/{id}/members`; added required indexes.

## Security Changes

Only active members can target a Community when publishing; media metadata is Cloudinary-path validated.

## Specs Added or Updated

Added `specs/features/communities.md`; updated Community database spec.

## Important Decisions

Community creation is exposed as a service/API; the roadmap does not require a separate creation screen.

## Known Limitations

Community media/live membership require configured services.

## Verification

Formatter, Svelte type-check, ESLint, unit tests, production build, API authorization smoke checks, and responsive browser smoke checks completed successfully.
