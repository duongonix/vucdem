# Phase 14

## Status

- [x] Completed

## Tasks

- [x] Implement typed Comment services/APIs, root pagination, replies, edit, soft-remove, and trusted counters.
- [x] Build composer, item/tree, capped visual nesting, Guest auth behavior, loading/empty/error/retry states, and Post Detail integration.

## Files Changed

- `src/routes/api/comments/**`, `src/lib/server/comments.ts`, `src/lib/services/comments.ts`, `src/lib/validation/comment.ts`
- `src/lib/components/comment/**`, Post Detail integration, `firestore.indexes.json`.

## Dependencies Added

- None.

## Database Changes

- Added two documented Comment composite indexes. Post `commentCount` includes roots and replies; direct-parent `replyCount` is trusted.

## Security Changes

- Server validates active identity, ownership, readable published target, parent relationship, and protected fields; delete is a continuity-preserving soft removal.

## Specs Added or Updated

- Added `specs/features/comments.md`; updated Comment schema and index specs.

## Important Decisions

- Phase 14 mutation scope is Post-only; the shared target type is retained for future Story/Chapter phases.
- Root pages include two reply generations and cap visual indentation at three levels.

## Known Limitations

- Live authenticated Firestore verification and large-tree performance testing are deferred until the configured full test pass.

## Verification

- Comment UI was later aligned with `specs/ui/img/comment.png`: split header, composer, actions, rules, skeleton, and nested thread components; comment voting now uses a trusted transactional API.

- Formatter, type-check, lint, and production build pass in the combined Phase 11–14 gate.
