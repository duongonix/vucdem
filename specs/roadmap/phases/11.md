# Phase 11

## Status

- [x] Completed

## Tasks

- [x] Replace the design preview home route with the published Post Feed.
- [x] Add reusable Post card, author metadata, Tags, responsive vote/action layout, filters, sorting, cursor Load More, deduplication, loading, empty, retry, and end states.
- [x] Deliver transformed lazy Cloudinary thumbnails without N+1 Author reads.

## Files Changed

- `src/lib/components/feed/HomeFeed.svelte`
- `src/lib/components/post/{PostCard,AuthorMeta,TagList}.svelte`
- `src/lib/utils/post.ts`, `src/routes/+page.svelte`

## Dependencies Added

- None.

## Database Changes

- None; Phase 9 Post indexes cover these queries.

## Security Changes

- Feed API remains published-only and returns denormalized author snapshots.

## Specs Added or Updated

- Reused `features/home-feed.md`, `ui/screens/home.md`, and `ui/img/home.png`.

## Important Decisions

- Filtered feeds intentionally use newest sorting, matching the documented MVP index boundary.
- Bookmark affordance is visibly non-active until requested Phase 15 rather than pretending persistence.

## Known Limitations

- Live Firestore feed verification requires configured credentials.

## Verification

- Formatter, type-check, lint, and production build pass in the combined Phase 11–14 gate.
- Production preview returns HTTP 200 for `/` with the canonical Feed title.
