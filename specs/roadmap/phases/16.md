# Phase 16

## Status

- [x] Completed

## Goal

Implement safe User-to-User following with accurate trusted counters.

## Tasks

- [x] Create/delete symmetric following/follower documents atomically.
- [x] Prevent self-follow and inactive-account mutations.
- [x] Keep `followingCount` and `followersCount` idempotent and non-negative.
- [x] Add trusted API, client service, optimistic Follow control, Guest routing, rollback, and Profile integration.

## Files Changed

- `src/routes/api/users/[username]/follow/+server.ts`
- `src/lib/services/follows.ts`
- `src/lib/components/profile/FollowButton.svelte`
- `src/lib/components/profile/ProfilePage.svelte`
- `specs/features/following.md`

## Dependencies Added

- None.

## Database Changes

- Implements existing `users/{uid}/following/{targetUid}` and mirrored `followers/{uid}` documents; no new fields or indexes.

## Security Changes

- Acting UID is token-derived; target resolution is trusted; self-follow and inactive accounts are rejected server-side.

## Specs Added or Updated

- Added `specs/features/following.md`.

## Important Decisions

- The canonical following-side document controls idempotent counter transitions; mirrored relation drift cannot produce duplicate increments.

## Known Limitations

- Follower/following listing screens and Story following belong to later phases.

## Verification

- Formatter, type-check, lint, unit tests, production build, and responsive Profile browser smoke pass in the combined Phase 15–17 gate.
