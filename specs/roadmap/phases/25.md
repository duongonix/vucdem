# Phase 25 — Notifications

## Status

Completed

## Tasks đã hoàn thành

Added private activity feed, unread header badge, mark-one/all-read operations, and trusted producers for comment, reply, user follow, upvote, and story updates.

## Files Changed

`src/lib/server/notifications.ts`, `src/lib/services/notifications.ts`, `src/routes/api/notifications/**`, `src/lib/components/notification/NotificationPage.svelte`, producer endpoints, `AppHeader.svelte`

## Dependencies Added

None.

## Database Changes

Added Notification query indexes and deterministic relation-event IDs.

## Security Changes

Only the recipient can query or mark records read; actors/targets/timestamps are server-produced; self-notifications are suppressed.

## Specs Added or Updated

Added `specs/features/notifications.md`; updated Notification database spec.

## Important Decisions

Mention records are supported by the model but only created once mention parsing is introduced by a later feature.

## Known Limitations

Unread badge refreshes on auth initialization rather than realtime listening.

## Verification

Formatter, Svelte type-check, ESLint, unit tests, production build, API authorization smoke checks, and responsive browser smoke checks completed successfully.
