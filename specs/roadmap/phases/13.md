# Phase 13

## Status

- [x] Completed

## Tasks

- [x] Implement all Post vote transitions, current-state reload, shared vertical/horizontal control, optimistic feedback, rollback, and Guest login routing.
- [x] Keep vote documents and trusted score atomic and idempotent.

## Files Changed

- `src/routes/api/posts/[id]/vote/+server.ts`
- `src/lib/services/votes.ts`
- `src/lib/components/post/VoteControl.svelte`
- Feed/detail integrations and `specs/features/voting.md`.

## Dependencies Added

- None.

## Database Changes

- Uses existing `posts/{postId}/votes/{uid}` documents; zero remains absence.

## Security Changes

- Authenticated server transaction owns score mutation and rejects non-published targets.

## Specs Added or Updated

- Added `specs/features/voting.md`.

## Important Decisions

- Trusted server endpoints provide the required authorization boundary while skipped Phase 6 rules remain unavailable.

## Known Limitations

- Story/Comment voting is outside this phase; live transaction verification requires Firebase credentials.

## Verification

- Static gates and production build pass; all transition math is centralized as `next - previous`.
