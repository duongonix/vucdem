# Phase 23 — Story Following

## Status

Completed

## Tasks đã hoàn thành

Added independent story follow/unfollow persistence, User-side relation, transactional count, detail-page action, and story-update production.

## Files Changed

`src/lib/services/story-follows.ts`, `StoryFollowButton.svelte`, `src/routes/api/stories/[id]/follow/+server.ts`

## Dependencies Added

None.

## Database Changes

Added `stories/{id}/followers/{uid}` and `users/{uid}/followedStories/{storyId}` relations.

## Security Changes

Authentication and public-story status are verified server-side; repeat mutations are idempotent.

## Specs Added or Updated

Updated notification behavior contract.

## Important Decisions

Story following is separate from following the author.

## Known Limitations

Live transaction verification requires Firebase credentials.

## Verification

Formatter, Svelte type-check, ESLint, unit tests, production build, API authorization smoke checks, and responsive browser smoke checks completed successfully.
