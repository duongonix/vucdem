# Phase 41 — Production Configuration

Status: In Progress

## Tasks completed

- Selected Vercel as the supported SvelteKit target through environment detection.
- Added an explicit default Firebase project and production deployment documentation.
- Audited the local environment variable names and public/server-only boundaries without exposing values.

## Files Changed

- `vite.config.ts`
- `.firebaserc`
- `docs/deployment.md`
- `package.json`
- `pnpm-lock.yaml`
- `specs/implementation/environment-variables.md`
- `specs/roadmap/phases/41.md`

## Dependencies Added

- None. The existing `@sveltejs/adapter-auto` selects Vercel in its production build environment.

## Database Changes

- No schema changes. Existing Firestore Rules and composite indexes are prepared for deployment.

## Security Changes

- Production secrets remain server-only and excluded from source control and browser bundles.

## Specs Added or Updated

- Documented Vercel deployment and the complete production environment contract.

## Important Decisions

- Vercel is the canonical SvelteKit hosting target; Firebase continues to provide Auth and Firestore, while Cloudinary remains the only media provider.
- `adapter-auto` is retained because the Vercel adapter's local Windows packaging requires symlink privileges; Vercel still selects its supported adapter during deployment.

## Known Limitations

- Production provider/domain configuration and live connectivity must be verified before completion.

## Verification

- Pending production build, provider checks, and secret-bundle audit.
